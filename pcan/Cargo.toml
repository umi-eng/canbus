[package]
name = "pcan"
description = "PEAK PCAN-USB adapter driver"
version = "0.1.0"
edition = "2024"
authors = ["Liam Kinne <liam@umi.engineering>"]
readme = "README.md"
repository = "https://github.com/umi-eng/canbus"
license = "MPL-2.0"

[dependencies]
embedded-can = "0.4"
thiserror = "2.0"
futures = "0.3"
nusb = "0.2.3"
zerocopy = { version = "0.8", features = ["derive"] }

[dev-dependencies]
tokio = { version = "1", features = ["full"] }

[features]
default = ["tokio"]
tokio = ["nusb/tokio"]
smol = ["nusb/smol"]
