[package]
name = "cansocket"
version = "0.1.0"
edition = "2024"

[dependencies]
async-io = "2.6"
embedded-can = "0.4"
libc = "0.2"

[dev-dependencies]
tokio = { version = "1.50", features = ["full"] }
